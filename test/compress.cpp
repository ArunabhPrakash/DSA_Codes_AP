#include<iostream>
#include<string.h>
#include<vector>
using namespace std;


int compress(vector<char>& chars) {
	n=chars.size();
	for(int i=0;i<n;i++){
		if(char[i])
	}
        
}

int main(){
	vector<char> chars={'a','a','b','b','c','c','c'};
	compress(chars);
	
}
